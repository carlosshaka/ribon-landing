<%= form_for(letter) do |f| %>
  <% if letter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(letter.errors.count, "error") %> prohibited this letter from being saved:</h2>

      <ul>
      <% letter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label 'Logo url' %>
    <%= f.text_field :logo %>
  </div>

  <button class="js-add-paragraph">Adicionar paragrafo</button>


  <div class="js-body-letter">
    <textarea class="js-paragraph"></textarea>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
